name: workflow.yml
on:
    push:
        branches:
            - main
            - feat/prisma

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6.1.0
            - run: pnpm --version
            - run: pnpm install
            - run: pnpm run test

